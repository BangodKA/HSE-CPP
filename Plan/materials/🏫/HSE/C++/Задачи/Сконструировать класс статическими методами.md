## Условие
Приватный конструктор, = delete, приватный деструктор и прочие варианты
Показать, что можно при этом конструировать класс статическими методами

## Решения
```c++
class CAbc
{
private:
    CAbc() { cout << "creating" << endl; }
public:
static CAbc& getInstance()
    {
        static CAbc _self;
            return _self;
    }
};

int main() {
    cout << "Begin" << endl;

    //CAbc::getInstance();
}
```

