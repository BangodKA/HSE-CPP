- Конструктор копирования и оператор присваивания
    - `a = b = c` – как и почему работает
	- Дефолтный генерируется, если не написал свой конструктор копирования
    - Rule dof 3
    -  Идиома copy-and-swap
	    - Лучше всего смотреть сразу на примере VectorInt
	    - Сначала конструктор копирования, потом operator=
	    - В operator= 3 проблемы
		    - Висячий указатель
		    - Утечка памяти
		    - Удалили сами себя
		- Как-то решить эти проблемы самостоятельно
		- И тут показать copy-and-swap
			```c++
			A& operator=(const A& a) {
				A temp = a;
				std::swap(data_, temp.data_);
				size_ = temp.size_;
				capacity_ = temp.capacity_;
			}
			```
	- https://web.archive.org/web/20140113221447/http://cpp-next.com/archive/2009/08/want-speed-pass-by-value/

- Перегрузка операторов
    - Операторы-члены класса/Операторы-свободные фукции
	    - https://stackoverflow.com/questions/43089272/friend-vs-member-functions-in-operator-overloading-c
    - Операторы сравнения, `<=>`
    - Операторы приведения типов `explicit` (тип определяется по названию)
	    - `explicit operator int() const {`
				`return a_;`
		 `}`
    - https://en.cppreference.com/w/cpp/language/operators
